name: Build AdGuardHome rules

on:
  schedule:
    - cron: "0 0 * * *"
    - cron: "0 12 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download rules
        run: |
          mkdir -p download
          RULES=(
            "https://raw.githubusercontent.com/BlueSkyXN/AdGuardHomeRules/master/all.txt"
            "https://raw.githubusercontent.com/BlueSkyXN/AdGuardHomeRules/master/skyrules.txt"
            "https://easylist-downloads.adblockplus.org/easylist.txt"
            "https://easylist-downloads.adblockplus.org/easylistchina.txt"
            "https://easylist-downloads.adblockplus.org/easyprivacy.txt"
            "https://anti-ad.net/easylist.txt"
            "https://raw.githubusercontent.com/8680/GOODBYEADS/master/data/rules/dns.txt"
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_49.txt"
            "https://filters.adtidy.org/android/filters/15_optimized.txt"
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_51.txt"
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_48.txt"
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_34.txt"
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_29.txt"
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_44.txt"
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_55.txt"
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_54.txt"
            "https://raw.githubusercontent.com/hagezi/dns-blocklists/main/adblock/light.txt"
            "https://filters.adtidy.org/android/filters/11_optimized.txt"
            "https://filters.adtidy.org/android/filters/17_optimized.txt"
            "https://filters.adtidy.org/android/filters/224_optimized.txt"
            "https://filters.adtidy.org/android/filters/14_optimized.txt"
            "https://filters.adtidy.org/android/filters/19_optimized.txt"
            "https://malware-filter.gitlab.io/malware-filter/phishing-filter-ag.txt"
            "https://urlhaus-filter.pages.dev/urlhaus-filter-ag-online.txt"
            "https://filters.adtidy.org/android/filters/228_optimized.txt"
            "https://filters.adtidy.org/android/filters/251_optimized.txt"
            "https://pgl.yoyo.org/adservers/serverlist.php?hostformat=adblockplus&mimetype=plaintext"
            "https://www.fanboy.co.nz/fanboy-antifacebook.txt"
            "https://fanboy.co.nz/fanboy-antifonts.txt"
            "https://ublockorigin.github.io/uAssetsCDN/filters/lan-block.txt"
            "https://adguardteam.github.io/AdGuardSDNSFilter/Filters/filter.txt"
            "https://raw.githubusercontent.com/Cats-Team/AdRules/main/dns.txt"
            "https://raw.githubusercontent.com/cjx82630/cjxlist/master/cjx-annoyance.txt"
            "https://raw.githubusercontent.com/xinggsf/Adblock-Plus-Rule/master/mv.txt"
            "https://raw.githubusercontent.com/damengzhu/banad/main/jiekouAD.txt"
            "https://raw.githubusercontent.com/TG-Twilight/AWAvenue-Ads-Rule/main/AWAvenue-Ads-Rule.txt"
            "https://hblock.molinero.dev/hosts_adblock.txt"
            "https://abp.oisd.nl/basic/"
            "https://raw.githubusercontent.com/StevenBlack/hosts/master/hosts"
            "https://someonewhocares.org/hosts/hosts"
          )

          for URL in "${RULES[@]}"; do
            FILENAME=$(basename "$URL")
            echo "Downloading $URL -> download/$FILENAME"
            curl -sSL "$URL" -o "download/$FILENAME" || echo "Failed: $URL"
          done

      - name: Merge & Dedup
        run: |
          mkdir -p dist
          awk '!/^(\s*!|#|$)/ {print $0}' download/* | sort -u > dist/AdGuardHome.txt
          {
            echo "! =========================================="
            echo "! 自建 AdGuard Home 合并去重规则"
            echo "! 更新时间：$(date -u +%Y-%m-%d\ %H:%M:%S) UTC"
            echo "! 来源：easylist, cjxlist, hagezi-pro 等"
            echo "! =========================================="
            cat dist/AdGuardHome.txt
          } > dist/AdGuardHome.tmp && mv dist/AdGuardHome.tmp dist/AdGuardHome.txt

          # 统计总规则数
          TOTAL_RULES=$(wc -l < dist/AdGuardHome.txt)
          echo "total_rules=$TOTAL_RULES" >> $GITHUB_ENV

          # 统计新增规则并保存高亮
          if [ -f dist/AdGuardHome.prev.txt ]; then
            comm -13 <(sort dist/AdGuardHome.prev.txt) <(sort dist/AdGuardHome.txt) > dist/AdGuardHome.new.txt
            NEW_RULES=$(wc -l < dist/AdGuardHome.new.txt)
          else
            cp dist/AdGuardHome.txt dist/AdGuardHome.new.txt
            NEW_RULES=$TOTAL_RULES
          fi
          echo "new_rules=$NEW_RULES" >> $GITHUB_ENV
          cp dist/AdGuardHome.txt dist/AdGuardHome.prev.txt

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        id: cpr
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "Update rules $(date -u +%Y-%m-%d)"
          branch: update-rules
          base: main
          title: "Update AdGuard Home rules $(date -u +%Y-%m-%d)"
          body: |
            自动合并更新规则  
            **总规则数**: ${{ env.total_rules }}  
            **新增规则数**: ${{ env.new_rules }}  

            **新增规则预览**（仅部分，最多 100 条）：  
            ```  
            $(head -n 100 dist/AdGuardHome.new.txt)  
            ```

      - name: Merge Pull Request
        if: steps.cpr.outputs.pull-request-number != ''
        uses: actions-ecosystem/action-pull-request@v2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          pull_request_number: ${{ steps.cpr.outputs.pull-request-number }}
          merge_method: merge

      - name: Show Rules Stats in Actions Log
        run: |
          echo "================= AdGuardHome Rules Update ================="
          echo "总规则数: ${{ env.total_rules }}"
          echo "新增规则数: ${{ env.new_rules }}"
          echo "新增规则（最多前100条）:"
          head -n 100 dist/AdGuardHome.new.txt
          echo "============================================================"
